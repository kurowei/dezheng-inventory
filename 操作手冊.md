# 得正物料盤點 App — 開發與更新操作手冊

給自己看的中文版流程說明。技術細節給 Claude Code 看的版本在專案裡的 `CLAUDE.md`。

> **這是獨立專案**：得正跟青山是兩個各自獨立的 GitHub repository、兩個獨立網址，
> 互不影響。改得正不會動到青山，反之亦然。青山那份操作手冊在
> `~/Documents/InventoryApp/qingshan-inventory/操作手冊.md`，兩邊流程邏輯完全一樣，
> 只是專案路徑和網址不同，別搞混資料夾就好。

---

## 專案位置

```
~/Documents/InventoryApp/dezheng-inventory
```

線上網址（店員在用的）：
```
https://kurowei.github.io/dezheng-inventory/
```

GitHub 倉庫：
```
https://github.com/kurowei/dezheng-inventory
```

---

## 日常修改流程（標準四步驟）

每次想調整功能、修 bug、換物料清單，都照這個順序走：

### 1️⃣ 開啟 Claude Code，說出你要的修改

```bash
cd ~/Documents/InventoryApp/dezheng-inventory
claude
```

進去之後，直接用中文描述需求，例如：
```
幫我在盤點頁面加上一個「清空重填」的按鈕
```

### 2️⃣ 本機預覽，確認改動符合預期

**不要急著推上線**，先用預覽腳本檢查：
```bash
./preview.sh
```
會印出兩個網址：
- 電腦上看：`http://localhost:8001`
- 手機上看（要跟電腦連同一個 Wi-Fi）：`http://<你的IP>:8001`

拿手機實際操作一次，確認畫面、按鈕、功能都正常。

**測試完，按 `Ctrl + C` 關掉本機伺服器。**

### 3️⃣ 確認沒問題後，推上 GitHub

回到 Terminal（或直接請 Claude Code 幫你跑）：
```bash
git add .
git commit -m "說明這次改了什麼，例如：新增清空重填按鈕"
git push
```

### 4️⃣ 等 1-2 分鐘，正式上線

GitHub Pages 會自動重新部署，等 1-2 分鐘後，用手機重新整理正式網址：
```
https://kurowei.github.io/dezheng-inventory/
```
確認新功能已經生效。之後不用重傳連結給店員，網址不會變，店員手機上「加入主畫面」的捷徑會自動抓到新版本。

---

## 如果不滿意，想繼續調整

回到步驟 1，繼續跟 Claude Code 說要改哪裡，重複「改 → 預覽 → 推送」直到滿意為止。**沒有 push 之前，正式網址上的店員完全不受影響**，可以放心慢慢調。

---

## 新增/更新物料品項與單價

**架構已經改過**：品號/品名/規格/單位/單價/分類這些欄位**不再寫死在 `index.html`
裡**，改放在 Google Sheets「得正_盤點品項主檔」，透過 Google Apps Script 部署成一個
API（`index.html` 裡的 `ITEMS_API_URL` 常數指到這個網址），App 一開啟就會連線抓取
最新資料。這樣單價資料才不會出現在公開的 GitHub repo 裡。

### 調整單價 / 新增品項 / 改品名規格

**直接打開 Google Sheets「得正_盤點品項主檔」編輯即可**，不用透過 Claude Code，也不用碰
`index.html`：

- 調整單價：改對應那一列的 `price` 欄位
- 新增品項：在最下面新增一列，填 `sku`、`name`、`spec`、`unit`、`price`、`category`
  （`最後叫貨日期` 欄位是紀錄用，可留空或照慣例填日期）
- 改品名/規格：直接改對應儲存格

存檔後**不用 git push、也不用等 GitHub Pages 重新部署**——App 下次開啟或重新整理時就會
自動抓到最新資料，最快幾秒鐘就生效。如果手機當下沒有網路，App 會自動退回上次成功
抓取、存在手機本機的離線快取資料繼續運作，並在畫面上顯示提示（盤點中畫面標題旁的
燈號也會從綠色變橘色）。

### 新增/調整「拆分數量輸入」的包裝規格

有些品項可以在盤點時把數量拆成「整數單位」+「零散秤重或散裝數量」分開輸入，
系統自動換算成含小數的精確金額（例如紅茶 600g/包，可以輸入「3包 + 200克」，
自動算出 3.33 包對應的金額）。

這組包裝規格（`unit_weight_g`、`pack_weight_g`、`packs_per_unit`、`sub_unit` 等欄位）
**跟單價不同，是寫死在 `index.html` 裡的 `PACKAGING` 物件**，因為這些資料不敏感、
也幾乎不會變動，不需要透過 Google Sheets 維護，改了也要透過標準「預覽 → 推送」
流程才會生效。

要新增或調整某個品項的包裝規格，直接在 Claude Code 裡描述該品項的包裝結構即可，例如：

```
這個品項是 3kg/包，6包/箱，幫我加上拆分數量輸入
```

Claude Code 會判斷該用哪組欄位（整包秤重／箱中有多包／純計數／三層計數），
改寫 `PACKAGING` 物件，然後照標準流程預覽測試再推送。

### 小提醒

- 單價/品項改動：改 Google Sheets，**不用 push**，App 重新整理就生效
- 包裝規格（拆分輸入）改動：改 `index.html` 的 `PACKAGING` 物件，**要走預覽 → 推送**
- 兩種改動都可以之後隨時再調整，不用擔心一次要做對

---

## 關掉 Claude Code 後，怎麼接續之前的進度

Claude Code 會自動記住每個專案資料夾裡的對話歷史，重開時可以直接接續，不用重講一次需求。

**步驟一：回到專案資料夾**

打開 Terminal（一般終端機，不是 Claude Code 裡面）：
```bash
cd ~/Documents/InventoryApp/dezheng-inventory
```

**步驟二：啟動並接續上一次的對話**
```bash
claude --continue
```
（簡寫 `claude -c`）會直接載入上次在這個資料夾裡對話的完整記錄，接著往下講就好。

**如果想挑選「哪一次」對話（不只是最近一次）**
```bash
claude --resume
```
（簡寫 `claude -r`）會跳出清單，列出這個資料夾過去的對話紀錄，用方向鍵選要接續哪一段。

**如果只是想開一個全新對話**（不接續之前的）
```bash
claude
```
直接這樣打就好，不用加參數。

**小提醒**：對話歷史是依「專案資料夾」區分的，只要在同一個 `dezheng-inventory` 資料夾裡執行，就找得到這裡的紀錄；換到別的專案資料夾，看到的會是那個專案自己的對話歷史。

---

## 常見情境對照表

| 我想做的事 | 該做什麼 |
|---|---|
| 改功能、修 bug | 走標準四步驟 |
| 調整單價、新增品項、改品名規格 | 直接改 Google Sheets「得正_盤點品項主檔」，存檔即生效，不用 push |
| 新增/調整拆分數量輸入的包裝規格 | 見上方「新增/更新物料品項與單價」章節，跟 Claude Code 說明包裝結構 → 預覽 → 推送 |
| 只是想看看目前線上版本長怎樣 | 直接用手機開 `https://kurowei.github.io/dezheng-inventory/`，不用跑 preview.sh |
| 想確認本機改動有沒有存好，但還沒想 push | 跑 `./preview.sh` 看效果即可，不用執行 git 指令 |
| 電腦重開機、忘記在哪個資料夾 | 先 `cd ~/Documents/InventoryApp/dezheng-inventory` 再開始 |

---

## 給 GitHub 推送用的身分

這台 Mac 已經用 `gh auth login` 完成 GitHub 授權（帳號：kurowei），**不用每次重新登入**，直接 `git push` 就能用。

如果哪天出現「認證失敗」「Device not configured」之類的錯誤，代表授權過期或換了新電腦，重新跑一次：
```bash
gh auth login
```
跟著畫面選 GitHub.com → HTTPS → Login with a web browser，用瀏覽器登入授權即可。

---

## 小提醒

- 每次改完**務必先 `./preview.sh` 用手機測過**再推送，不要跳過這步直接 push，避免店員直接遇到有問題的版本
- commit message 建議寫清楚這次改了什麼，之後回頭找歷史紀錄比較好對照（例如 `git log` 可以看到所有修改紀錄）
- 資料是存在**店員手機的瀏覽器本機**，不是雲端資料庫，所以修改 `index.html` 本身不會影響店員手機上已經存的歷史盤點紀錄
- 「盤點中」畫面標題旁有個小燈號，反映連線狀態：綠色＝連線正常抓到最新資料、橘色＝連線失敗改用手機本機的離線快取、紅色＝連線失敗又沒有快取可用。如果店員回報看到橘燈或紅燈，代表當下網路不穩，可請他們檢查 Wi-Fi
